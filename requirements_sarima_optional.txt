# Optional dependencies for enhanced SARIMAX performance

# Auto-ARIMA (intelligent order selection) - RECOMMENDED
pmdarima>=2.0.0

# GPU acceleration (RAPIDS) - OPTIONAL
# Requires CUDA-capable GPU
# Install via conda instead:
#   conda install -c rapidsai -c conda-forge -c nvidia cudf=24.10 cupy cuda-version=12.0

# Parallel processing - REQUIRED for accelerated version
joblib>=1.3.0
